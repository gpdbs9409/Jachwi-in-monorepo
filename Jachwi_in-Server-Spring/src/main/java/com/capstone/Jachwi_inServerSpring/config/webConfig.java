package com.capstone.Jachwi_inServerSpring.config;

// CORS 설정은 CorsConfig.java 로 이전됨 (이 클래스는 @Configuration이 없어 실제로는
// Spring Bean으로 등록되지 않던 죽은 코드였음). 삭제 권한이 없어 내용만 비워둠 —
// 정리하고 싶다면 이 파일을 지워도 무방함.
